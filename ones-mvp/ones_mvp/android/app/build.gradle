plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "events.ones.mvp"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "events.ones.mvp"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
    lintOptions {
        disable 'NewApi'
    }
}

flutter {
    source = "../.."
}

// 🔹 Agrega Firebase y Google Services en las dependencias
dependencies {
    implementation platform('com.google.firebase:firebase-bom:32.1.0') // 🔹 Última versión de Firebase BOM
    implementation 'com.google.firebase:firebase-auth' // 🔹 Autenticación de Firebase
    implementation 'com.google.android.gms:play-services-auth:20.7.0' // 🔹 Google Sign-In
}

// 🔹 Asegurar que Google Services se procese antes de que ocurra `mapReleaseSourceSetPaths`
afterEvaluate {
    preBuild.dependsOn processReleaseGoogleServices
}

// 🔹 Agrega el plugin aquí, al final del archivo:
apply plugin: 'com.google.gms.google-services'
